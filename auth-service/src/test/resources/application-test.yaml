spring:
  flyway:
    enabled: true
    driver-class-name: org.postgresql.Driver
    locations: classpath:/db/migration, classpath:/db/testdata
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql